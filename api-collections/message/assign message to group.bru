meta {
  name: assign message to group
  type: http
  seq: 2
}

post {
  url: {{host}}/messages/{id}/assign-message-group
  body: json
  auth: inherit
}

body:json {
  {
    "text": "asddasd"
  }
}

settings {
  encodeUrl: true
}
